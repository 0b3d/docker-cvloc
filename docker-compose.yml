version: "3"
services:
    postgis:
        #image: geogra
        build: ./postgis
        ports: 
            - "5432:5432"
        volumes:
            - db-data:/data
        environment: 
            - POSTGRES_PASSWD=postgres
            - LOCALE=en_GB
volumes:
    db-data:
